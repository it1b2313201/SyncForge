# Frontend

This folder contains the React + Vite frontend for the Kanban app.

## Run frontend

1. Open PowerShell in `frontend/`.
2. Install dependencies:
   ```powershell
   npm install
   ```
3. Start the dev server:
   ```powershell
   npm run dev
   ```

The app fetches tasks from `/api/tasks` and supports moving cards between columns.
